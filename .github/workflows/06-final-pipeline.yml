name: 06 - Final Pipeline with GitHub Pages

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
    paths:
      - '06-final-pipeline/**'
      - '.github/workflows/06-final-pipeline.yml'

jobs:
  test:
    name: Final Pipeline Tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./06-final-pipeline
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests with HTML report
        run: npx playwright test --reporter=html
        continue-on-error: true

      - name: Upload HTML report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-06-final
          path: 06-final-pipeline/playwright-report/
          retention-days: 30

      - name: Upload report for GitHub Pages
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: 06-final-pipeline/playwright-report/

  deploy:
    name: Deploy to GitHub Pages
    needs: test
    if: always()
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
